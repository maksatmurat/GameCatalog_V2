@page "/gamesdetails/{GameId:int}"
@page "/gamesdetails/{GameId:int}/{GameName}"

<h3>Game Details</h3>
@code
{
    [Parameter] public int GameId { get; set; }
    [Parameter] public string? GameName { get; set; }
}